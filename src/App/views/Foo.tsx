import React from "react"

const Foo: React.FC = ({}) => {
    return (
        <div className="card">
            <div className="card-body">
                <button className="btn btn-primary">Foo</button>
            </div>
        </div>
    )
}

export default Foo
